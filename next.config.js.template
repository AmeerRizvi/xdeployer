/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable output standalone for PM2 deployment
  output: 'standalone',
  // Add any other Next.js configuration options here
};

module.exports = nextConfig;
